pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "mataBot"
include ':app'
include ':OpenCV4'

dependencyResolutionManagement {

    repositories {

        maven { url 'https://jitpack.io' }
    }
}